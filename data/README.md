# data/LLMS.json

This folder contains structured data used for SKS Solution.

- `LLMS.json` — JSON representation of company information, products, services, keywords and metadata generated on 2026-02-28.

Usage:
- Can be consumed by build scripts, API endpoints, or CMS imports.
- Keep in sync with `LLMS.txt` if content is updated.

Notes:
- Public path: `/data/LLMS.json` when deployed at the site root.
- If you want this file indexed by search engines, ensure `sitemap.xml` includes this path.
